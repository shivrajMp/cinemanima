import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2196f3', // MIUI blue color
    },
    secondary: {
      main: '#ff4081', // MIUI pink color
    },
  },
});

export default theme;